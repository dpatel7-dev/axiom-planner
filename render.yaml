services:
  - type: web
    name: axiom-planner
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: axiom-db
          property: connectionString
      # ↓ Set these in Render's dashboard after first deploy
      - key: OWNER_USERNAMES
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

databases:
  - name: axiom-db
    plan: free
    databaseName: axiom
    user: axiom
